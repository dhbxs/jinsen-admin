server:
  port: 8181

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jinsen-exercise?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2b8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  main:
    allow-bean-definition-overriding: true

mybatis-plus:
  # 配置mapper扫描位置
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # 配置控制台打印mybatis sql语句
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl